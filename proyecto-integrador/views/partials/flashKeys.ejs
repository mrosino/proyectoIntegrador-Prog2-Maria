<% for (const key in locals.flash) { %>
    <% for (const content of flash[key]) { %>
        <div class="alert alert-<%= key %>" role="alert">
            
        <%= content %>
     </div>
        
    <%  } %>
 <% } %>
